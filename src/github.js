const accessTokens = {};

const {
  GITHUB_URL = 'https://github.com',
  GITHUB_API_URL = 'https://api.github.com',
} = process.env;

const fetchPRLintJson = function () {};
const postComment = function () {};

module.exports = {
  fetchPRLintJson,
  postComment,
};
